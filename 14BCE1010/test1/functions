lower = int(input());
upper = int(input());
def sum(lower,upper):
    sum1 = 0
    while lower <= upper:
          sum1 = sum1+lower;
          lower = lower+1;
    return sum1; 
c = sum(lower,upper);
print (c)


     